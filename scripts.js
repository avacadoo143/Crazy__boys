document.getElementById('login').addEventListener('submit', function(event) {
    event.preventDefault();
    const email = document.getElementById('email').value;

    if(email == "supyaaaaa123@gmail.com") {
        window.location.href="login.html"
    } else {
        window.location.href="login.html"
    }
});
